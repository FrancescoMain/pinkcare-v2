# Local development configuration (local Postgres or Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=PINKCARE_DB
DB_USER=postgres
DB_PASSWORD=pinkcare2025
DB_SSL=false

# JWT Configuration
JWT_SECRET=dev-secret-key
JWT_EXPIRES_IN=7d

# Email (optional in local)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=PinkCare Dev <no-reply@localhost>

# App settings
NODE_ENV=development
PORT=3001
CORS_ORIGIN=http://localhost:3000
APP_URL=http://localhost:3000
